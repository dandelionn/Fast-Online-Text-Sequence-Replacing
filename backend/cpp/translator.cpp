#include "translator.h"
#include "modern.h"

std::string ProccessInput(std::string dictionaryPath, std::string inputPath, std::string outputPath)
{
    std::string result = "";
    auto i = 0UL;
    //execute();
    /*for(; i<10000; ++i) 
    {
        result += dictionaryPath;
        result += inputPath;
        result += outputPath;
    }
    result += i;
    */    

    //result += "Done executing the c++ code!";

    return result;
}